### Q: 为什么组件中data必须是function?

**A: `组件是用来复用的，组件的本质是对象`。根据这两点，我们看到一个组件A中data若是对象，则页面中所有组件A的data都引用同一块数据，这显然不是我们想要看到的，使用function来生成一份拷贝，让每个组件A的data都保持独立**。


### Q: 为什么new Vue中的data可以是对象?

**A: new Vue是页面中的根组件，在页面中`唯一`，data是对象不会对数据造成影响**
